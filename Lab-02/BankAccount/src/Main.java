/* a)Account name (string), (int) account balance , (string) credit history
   b)deposit(amount) == updates balance , withdraw(amount) == updates balance , check(balance) 
   c) THe main problem arises when we start handling 200 customers , it would be really hard to manage the users and their
   expected requests like manage the account , withdraw , checking balance.
   d)the most logical thing I would do is add a restriction like a transaction limit to the BankAccount class , which will only affect that single customer
   and no one else
*/
public class Main {
    public static void main(String[] args) throws Exception {
        
    }
}